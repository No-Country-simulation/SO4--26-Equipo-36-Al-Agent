services:
  # Servicio de la Aplicación FastAPI
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: conversa-ai-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      chromadb:
        condition: service_started
    networks:
      - conversa-network
    volumes:
      - .:/code
      - app_hf_cache:/root/.cache/huggingface
      - app_chroma_cache:/root/.cache/chroma
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Base de Datos PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: conversa-ai-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-conversa_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-conversa_pass}
      POSTGRES_DB: ${POSTGRES_DB:-conversa_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-conversa_user} -d ${POSTGRES_DB:-conversa_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - conversa-network

  # ChromaDB Server (Vector Store para RAG)
  chromadb:
    image: chromadb/chroma:latest
    container_name: conversa-ai-chromadb
    ports:
      - "8100:8000"
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    networks:
      - conversa-network

  # pgAdmin (GUI de PostgreSQL)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: conversa-ai-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@conversa.ai}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin_pass}
    ports:
      - "5050:80"
    depends_on:
      - db
    networks:
      - conversa-network
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
    driver: local
  chroma_data:
    driver: local
  pgadmin_data:
    driver: local
  app_hf_cache:
    driver: local
  app_chroma_cache:
    driver: local

networks:
  conversa-network:
    driver: bridge
